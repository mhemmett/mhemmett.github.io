name = "rag-mhemmett-mhemmett-github-io"
main = "src/index.js"
compatibility_date = "2025-04-01"

[vars]
ALLOWED_ORIGIN = "https://mhemmett.github.io"

# Secrets (set via `wrangler secret put`, do NOT commit values):
#   wrangler secret put GEMINI_API_KEY
#   wrangler secret put CHAT_PASSWORD
