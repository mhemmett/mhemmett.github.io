# January 27, 2026

Part of [[Axial-Splitting]].

William and I discussed the Baillard method, which does not include windowing and clustering like Teanby, and agreed that I can add that component after I've exactly replicated his results, though it's just over one window.

I've made no additional progress aligning my SNR results with Baillard's. I have also made edits to the actual splitting workflow, and used Claude and Baillard scripts to write his functions and processing into my workflow and with my data structure.

After correcting some hyperparameters to match Baillard, and ensuring that fast direction is over the same window (±90°), my results still show discrepancies in both splitting parameters. In the case of delay time, it's a discrepancy of about 3 samples or so at maximum — which is encouraging! That's not a huge difference, given the maximum lag allowed is 60 samples. We're talking about a discrepancy within 5% of the total range.

The fast direction is a slightly different story. I've seen in output plots from SWSPy that many of these splitting parameter spaces appear to be mostly symmetric about fast direction = 0°. This leads to a ± discrepancy in the chosen fast direction, and then on top of that there's up to 10° of error from Baillard's results.

The next step is to plot each parameter space, showing points for my results using an adaptation of Christian's method and then to plot each "ground truth" point from Christian's original results. Then we can see:

- Where are the actual minima, and does Christian's method find them?
- Does my adaptation of Christian's method find them?
- Are the global minima large enough that ± a few degrees or samples is a good enough estimate to correct the splitting?
- Is there, in fact, a reflection-plane symmetry about fast direction = 0? If not, is it just a coincidence that I keep getting results so close to Christian's but in the opposite sign?
- Also, test on more data.

I see that Christian had a variable `add_lag = 3` in `run_sws.py`. Apparently his method would miss extrema at the edges of the parameter space, so he calls `add_lag` for min and max lag in `get_LAMBDAS`, but not in `process_LAMBDAS` so as to stay in the 60-sample range.

Now that I've added that, I'm prompting Claude to make a plan for integrating Baillard-like output plots to the event loop, so we can actually see the outputs and see if the selected minima make sense.

I've added Baillard's output plots, but they are currently buggy and not very interpretable, so I am fixing them. We'll be able to see the top two min lambdas and see if they actually fall in the correct minima. If not, we have some issues. I will look at Christian's output plots and see if his results actually fit the minima as well.

After attempting to fix the plots and then plotting them again to show our results vs. Christian's results in phase space, it is unclear where the actual minima are.

I've corrected the plotting code for the most part, as well as the minima calculation code so it uses Christian's method exactly — yet, there are still major discrepancies between my results and his. I am not yet sure why, but having clear visualizations will help to guide the process.

SNR is still confusing to me — some events are almost exactly the same, but there are a few others with a larger discrepancy, and I don't understand why.
