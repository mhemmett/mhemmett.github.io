# January 20, 2026

Part of [[Axial-Splitting]].

Since I last updated this notebook, I've made significant progress.

First, **back-azimuth is exactly identical**. I've integrated Christian's scripts, catalogs, and station locations (from IRIS MDA — I should have done that from the start, but now I know!).

**S-wave SNR** is close — but something is not quite right. I find that I can correctly calculate it for the first event, but then there's various levels of divergence with subsequent events. I wonder if it has to do with how the taper and filter is being applied onto the trace, or how the data is retrieved from the trace. Worth investigating; I think there's a bug somewhere.

**Angle of incidence and rectilinearity** — these are now (within a few decimal points) correct! After I implemented Christian's method on the P-wave window and made sure to apply the right tapering and filtering (I can't believe I forgot to do that!), I'm seeing results that are pretty consistent. Again though, there are some issues with the first event. How curious that the one event where SNR is correct, the angle of incidence and rectilinearity are incorrect. That must not be an accident. Is the problem with the data, or the process? What happens if I exclude the first event from the data and then run the analysis again? Does the second event get screwed up?

Looking at Christian's results for the (second) event, I see consistency in rectilinearity, incidence, back-azimuth, and discrepancy in SNR, as discussed. Looking at the actual splitting results, I see a couple points of discrepancy.

First, my **δt value is 0.030 seconds**, while Christian's results are **5.56 samples**. Now, for our broadband OBS on the OOI RCA, we have a sampling rate of 200 Hz. So 0.03 seconds corresponds to 6 samples — and this is great! That's not a huge discrepancy from Christian's results, but it does tell me there's a way that MFAST 2.0 is doing windowing and clustering on a different scale, leading to greater precision on the part of MFAST. I might need to add a row to the table, but right now it's unclear.

Secondly, my **fast direction is −3.0°** while Christian's results are **−81.0°**. The thing is, visually looking at my output parameter space for this event, I can see that my result is not sitting in a global minimum, and that there IS a global minimum at ±81.0°, and at approximately 5.5–6.0 samples. So this is likely due to the constraint on clustering. MFAST 2.0 has a minimum number of 5 data points per cluster, but before when I tried to put 5 data points into one cluster, I got invalid results. For the time being I put one data point per cluster, but that's causing the final result to get caught in a local minimum. So I'll try increasing `Ncmin` toward 5 and see what results I get.

Okay, so it seems that some events have up to 3 points per cluster (`Ncmin = 3` in `split.py`), but not all, and none had at least 4 points per cluster. So I think this is also related to how we define the parameter space. If we have a finer grid (i.e. on the order of hundredths of samples), then we should theoretically have more clustering precision. So that is definitely something that needs to be fixed.
